[package]
name = "doc-engine-scaffold"
version = "0.1.0"
edition = "2021"

[lib]
name = "doc_engine_scaffold"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yml = "0.0.12"
regex = "1"

[dev-dependencies]
tempfile = "3"
