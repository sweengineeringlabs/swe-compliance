# Manual Test Execution: CLI Interface

> **TLDR:** Manual test checklist for CLI Interface — step-by-step procedures with expected outcomes.

**Version:** 1.0
**Status:** Pending
**Test Plan:** [Test Plan](cli_interface.test)

---

## Test Cases

| TC | Test | Steps | Expected |
|----|------|-------|----------|
| TC-001 | FR-500: Scan command (Demonstration) | Execute `doc-engine scan <PATH> --scope <TIER>` and observe output | `doc-engine scan <PATH> --scope <TIER>` executes a scoped scan and prints results |
| TC-002 | FR-501: JSON flag (Test) | Run `doc-engine scan <PATH> --json` | `doc-engine scan <PATH> --json` outputs valid JSON |
| TC-003 | FR-502: Check filter flag (Test) | _TODO_ | `--checks 1-13` runs exactly 13 checks; `--checks 1,2,3,14-25` runs exactly 15 |
| TC-004 | FR-503: Project type flag (Test) | _TODO_ | `--type internal` skips open-source-only rules; `--type open-source` is the default |
| TC-005 | FR-504: Rules file flag (Test) | _TODO_ | `--rules custom.toml` loads and uses the specified file; missing file produces exit code 2 |
| TC-006 | FR-505: Scope flag (Test) | _TODO_ | `--scope small` runs only small-tier checks, skipping medium and large; `--scope medium` runs small+medium; `--scope large` runs all; unknown values produce exit code 2 |
| TC-007 | FR-506: Output flag (Test) | _TODO_ | `--output <path>` or `-o <path>` writes a JSON report to the specified path; parent directories are created automatically; the recommended filename is `documentation_audit_report_v{version}.json` per ISO/IEC/IEEE 15289:2019 |

---

## Execution Log

| TC | Tester | Date | Pass/Fail | Notes |
|----|--------|------|-----------|-------|
| TC-001 | | | | |
| TC-002 | | | | |
| TC-003 | | | | |
| TC-004 | | | | |
| TC-005 | | | | |
| TC-006 | | | | |
| TC-007 | | | | |

