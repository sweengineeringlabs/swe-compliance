# Manual Test Execution: Spec Subcommand

> **TLDR:** Manual test checklist for Spec Subcommand — step-by-step procedures with expected outcomes.

**Version:** 1.0
**Status:** Pending
**Test Plan:** [Test Plan](spec_subcommand.test)

---

## Test Cases

| TC | Test | Steps | Expected |
|----|------|-------|----------|
| TC-001 | FR-750: Spec validate subcommand (Demonstration) | Execute `doc-engine spec validate <PATH>` and observe output | Validates all spec files under PATH and prints schema diagnostics |
| TC-002 | FR-751: Spec cross-ref subcommand (Demonstration) | Execute `doc-engine spec cross-ref <PATH>` and observe output | Analyzes cross-references and prints a categorized report |
| TC-003 | FR-752: Spec generate subcommand (Demonstration) | Execute `doc-engine spec generate <FILE> --output <DIR>` and observe output | Produces a markdown file from the given YAML spec |
| TC-004 | FR-753: Spec subcommand exit codes (Test) | _TODO_ | Exit code 0 = clean, 1 = violations found, 2 = error (same semantics as `scan`) |
| TC-005 | FR-754: Spec JSON output (Test) | Run `doc-engine spec validate <PATH> --json` | Outputs valid JSON deserializable to `SpecValidationReport`; `doc-engine spec cross-ref <PATH> --json` outputs valid JSON deserializable to `CrossRefReport` |
| TC-006 | FR-755: Spec text output (Demonstration) | Demonstrate via `core/reporter.rs` | Default (no `--json`) output is human-readable text with file paths, diagnostic messages, and a summary line |

---

## Execution Log

| TC | Tester | Date | Pass/Fail | Notes |
|----|--------|------|-----------|-------|
| TC-001 | | | | |
| TC-002 | | | | |
| TC-003 | | | | |
| TC-004 | | | | |
| TC-005 | | | | |
| TC-006 | | | | |

